{
  "name": "braindump-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"cd frontend && npm start\" \"cd backend && python main.py\"",
    "install-all": "npm install && cd frontend && npm install --legacy-peer-deps && cd ../backend && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 